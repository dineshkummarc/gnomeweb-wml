
Same rules apply as explained in general HACKING file (gnomeweb-wml module).

To build the bounty page:

        mcs build.cs
        mono build.exe
        for i in *.php3; do php $i > `echo $i | sed s/php3/html/g`; done

Then commit.

